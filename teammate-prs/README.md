# Scripted Teammate PRs (Chapter 23)

A small set of pre-built pull requests against CinéTrack used in chapter 23 (*Reviewing Other People's PRs with AI*). The reader uses AI as a co-reviewer on these PRs.

Each PR is its own git branch in this directory's local repo, with a commit message that hints at the kind of issue the AI should and should not catch.

Will be populated when chapter 23 ships.
